return {
  ensure_installed = { "lua", "luadoc", "printf", "vim", "vimdoc", "python", "nix", "javascript" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}
